# ⚡ Быстрый старт для mental.robotlida.by

## 🎯 Все готово! Вот что нужно сделать на продакшене:

### 📥 1. Подтяните изменения
```bash
cd /home/robotlidab/math  # или ваш путь
git pull origin main
```

### 📝 2. Настройте .env файл
```bash
# Создайте .env из шаблона
cp env_production.txt .env

# Домен уже настроен: mental.robotlida.by ✅
```

### ⚙️ 3. Выберите режим безопасности

#### БЕЗ SSL (текущий HTTP):
```bash
# Ничего менять не нужно, уже False
CSRF_COOKIE_SECURE=False
SESSION_COOKIE_SECURE=False
```

#### СО SSL сертификатом:
```bash
nano .env
# Измените на:
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True
```

### 🔧 4. Запустите исправление
```bash
python fix_csrf_error.py
```

Или вручную:
```bash
python manage.py collectstatic --noinput
touch tmp/restart.txt
```

### ✅ 5. Проверьте результат
Откройте в браузере:
- http://mental.robotlida.by/admin/

## 📊 Ответы на ваши вопросы

### ❓ True или False для продакшена?

**FALSE (False):**
- ✅ Используйте, если НЕТ SSL сертификата
- ✅ Работает по HTTP (без шифрования)
- ✅ Быстрее, проще настроить

**TRUE (True):**
- ✅ Используйте, если ЕСТЬ SSL сертификат
- ✅ Работает по HTTPS (с шифрованием)
- ✅ Безопаснее, но требует сертификат

### 🎯 Для вашего домена mental.robotlida.by:

**Сейчас (без SSL):** Используйте `False` ✅

**Если установите SSL позже:** Поменяйте на `True` ✅

## 🚀 Готово к деплою!

Все настройки уже сделаны:
- ✅ Домен mental.robotlida.by добавлен
- ✅ CSRF настроен правильно
- ✅ Security settings оптимизированы
- ✅ Статика будет собрана

Просто выполните команды выше! 🎉
